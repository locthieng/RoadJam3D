using UnityEngine;
using NgoUyenNguyen.GridSystem;

/// <summary>
/// Grid cụ thể dạng ô vuông, kế thừa từ BaseGrid.
/// </summary>
[ExecuteAlways]
public class BaseGridSquare : BaseGrid
{
    // Hiện tại chỉ dùng lại toàn bộ logic của BaseGrid.
    // Có thể mở rộng ở đây nếu muốn thêm tính năng riêng.
}